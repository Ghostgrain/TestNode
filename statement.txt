1.node 通过npm加载进项目的项目要 --save  更新package
2.stream zlib重复压缩会造成文件损坏
3.require模块的时候如果是文件模块最好带上./之类的路径
4.__filename 表示当前正在执行的脚本的文件名。它将输出文件所在位置的绝对路径.
5.__dirname 表示当前执行脚本所在的目录。
6.对文件或者数据的写都是耗时的,如果在这个写过程完成之前读文件或数据可能会发生错误